import React from 'react'

const Fallback: React.FC = () => {
  return (
    <div className="flex justify-center items-center h-screen" style={{ textAlign: 'center' }}>
      Loading...
    </div>
  )
}

export default Fallback
